<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/hijau_tombol1" /> <!-- Warna hijau -->
    <corners android:radius="5dp" />
    <stroke
        android:width="1dp"
        android:color="@color/hijau"
        android:dashGap="5dp"
        android:dashWidth="10dp"/>
</shape>
